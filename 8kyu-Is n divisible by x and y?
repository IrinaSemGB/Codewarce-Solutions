-- JavaScript solution --

const isDivisible = (n, x, y) => (n % x === 0 && n % y === 0);


-- Swift solution --

func isDivisible(_ n: Int, _ x: Int, _ y: Int) -> Bool {
  return n % x == 0 && n % y == 0
}
