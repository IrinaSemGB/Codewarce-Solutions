JavaScript solution

function century(year) {
  const cen = year / 100;
  return Math.ceil(cen);
}


Swift solution

func century(_ year: Int) -> Int {
  if (year % 100 > 0) {
    return year / 100 + 1;
  } else {
    return year / 100;
  }
}
