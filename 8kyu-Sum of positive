JavaScript solution

function positiveSum(arr) {
  let sum = 0;
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] > 0) {
      sum += arr[i];
    }
  }
  return sum;
}

Swift solution

func sumOfPositives (_ numbers: [Int] ) -> Int {
  let res = numbers.filter{$0 > 0}.reduce(0) {(a, b) in a + b}
  return res
}
